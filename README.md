# RAGE:MP Chromium Roleplay

### Before running server
Build `.ts` files:
``` bash
cd app/src/client
tsc
```
``` bash
cd app/src/server
tsc
```

### Recompile server files and start server
#### **Run `start.bat`**
